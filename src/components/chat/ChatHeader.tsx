"use client";
import React from 'react';

export const ChatHeader: React.FC = () => {
  return (
    <header role="banner" className="flex items-center justify-center h-[74px] px-10 border-b border-white/10">
      {/* Intentionally left simple after removing settings button */}
    </header>
  );
};
